# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
dist/
build/
*.egg-info/

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
.pytest_cache

# uv lock file (library project - don't commit)
uv.lock

# IDE specific files
.idea/
.vscode/
*.swp
*.swo

# Custom
/notebooks
/data
/examples

# Quarto documentation
docs/_site/
docs/.quarto/
.quarto/
# docs/_freeze/ is committed: it holds the frozen notebook outputs CI renders from
# (execute: freeze in docs/_quarto.yml). See docs/BUILDING.md.
docs/.jupyter_cache/
docs/**/.jupyter_cache/
docs/reference/*.qmd
docs/legacy
docs/_styles-quartodoc.css

# Keep workflow notebooks in source (needed for Colab links)
# Notebooks are now committed to enable Google Colab integration
# docs/workflows/*.ipynb
# docs/advanced/*.ipynb

# Keep tvboptim cache directories (pickle files from @cache decorator)
# These should be committed for reproducible builds
# !docs/**/cache/

# OS-specific
.DS_Store
.DS_Store?
._*
Thumbs.db

# AI Skills
src/tvboptim/claude/skills

# Stray cache dir from running doc notebooks outside their own directory
/cache/
